version: '3'
services:
  db:
    image: postgres:11.2
  api:
    build: .
    container_name: workinmena-api
    command: bin/rails s -p 3000 -b '0.0.0.0'
    volumes:
      - .:/app
    ports:
      - 3000:3000
    depends_on:
      - db
